{
	"version": "1.0.0",
	"name": "com.cotacoes.data_scrapper",
	"description": "A backend server that scrap data from messages received by message broker",
	"author": "Henrique Barros",
	"license": "ISC",
	"type": "module",
	"main": "main.ts",
	"scripts": {
		"start": "node --env-file=.env src/main.ts",
		"dev": "node --env-file=.env --watch src/main.ts",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"amqplib": "^0.10.9",
		"selenium-webdriver": "^4.37.0"
	},
	"devDependencies": {
		"@types/amqplib": "^0.10.7",
		"@types/node": "^24.3.0",
		"@types/selenium-webdriver": "^4.35.1"
	}
}